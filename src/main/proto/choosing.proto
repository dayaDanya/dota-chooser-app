syntax = "proto3";

package com.dotachooser.grpc;

message Heroes{
  repeated int32 myTeam = 1;
  repeated int32 enemyTeam = 2;
}

message Hero{
  int32 choice = 1;
}

service ChoosingService{
  rpc choosing(Heroes) returns (Hero);
}